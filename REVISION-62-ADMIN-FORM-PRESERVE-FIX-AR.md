# Revision 62 — إصلاح حفظ قواعد البيانات وعدم تفريغ الحقول

تم إصلاح سلوك لوحة الأدمن عند إنشاء حساب المشرف العام أو إضافة قاعدة Firestore جديدة.

## التعديلات

- عدم تفريغ حقول إعداد قاعدة البيانات تلقائياً، سواء نجح الحفظ أو فشل.
- حفظ مسودة حقول `firebaseConfig` في التخزين المحلي أثناء الكتابة، واستعادتها تلقائياً بعد إعادة فتح الصفحة أو إعادة تحميلها.
- إضافة اختبار اتصال لقاعدة الشركة قبل/أثناء الحفظ.
- إذا فشل اتصال قاعدة الشركة بسبب Authentication أو Rules، يتم حفظ تعريف القاعدة في القاعدة الرئيسية مع إبقاء الحقول ظاهرة لإصلاحها لاحقاً.
- التحقق من أن تعريف القاعدة ظهر فعلياً في Firestore الرئيسي بعد الحفظ.
- إضافة رسائل واضحة للأخطاء الشائعة: الصلاحيات، Anonymous Authentication، apiKey، Database ID، والشبكة.
- عدم إنشاء كاش محلي لحساب المشرف العام قبل نجاح حفظه في Firestore.
- إعادة قيم اسم المستخدم وكلمة المرور داخل نموذج المشرف عند فشل الحفظ داخل Android WebView.
- الإبقاء على بيانات القاعدة بعد النجاح، مع زر «قاعدة جديدة» لمسح الحقول يدوياً فقط.
- رفع إصدار Service Worker لمنع تحميل نسخة الأدمن القديمة من الكاش.
