# تقرير اختبار Revision 70

## الاختبارات المنفذة

- اجتازت جميع ملفات JavaScript فحص الصياغة عبر `node --check`.
- اجتاز 34 مقطع JavaScript مضمّناً في جميع صفحات HTML فحص الصياغة.
- اجتاز 27 مقطع JSON مضمّناً في صفحات HTML التحقق البنيوي.
- تم التحقق من عدم وجود `readAsDataURL` في مساري الشعار وتصميم الفاتورة.
- تم التحقق من تغيير فحص Firestore الدوري من 3 ثوانٍ إلى القيمة المهيأة، وهي 120 ثانية افتراضياً.
- تم التحقق من وجود Pagination باستخدام `limit()` و`startAfter()` في الجسر الرئيسي ومسار قواعد البيانات المتعددة.
- تم التحقق من تفعيل `persistentLocalCache` مع مدير تبويبات متعدد في مسار Multi Database.
- تم التحقق من وجود التخزين الأحادي `cashtop-inline-json-v2` للبيانات الصغيرة والتخزين المجزأ للبيانات الكبيرة.
- تم التحقق من عزل أخطاء Dataset وسجل التدقيق الدائمة وإزالة العناصر المعزولة من الطابور النشط.
- تم التحقق من إغلاق مستمع RTDB والمؤقتات عند إخفاء الصفحة أو مغادرتها.
- تم التحقق من أن `LOCAL_ASSETS` في Service Worker يحتوي 65 ملفاً دون ملفات مفقودة أو مكررة.
- تم تحديث إصدار Service Worker إلى `v70-firestore-cost-cache-queue`.
- تم تحديث معاملات إصدارات ملفات Firebase المشتركة في 36 صفحة HTML إلى `v=70` لمنع تشغيل ملفات قديمة من كاش المتصفح.

## فحوص سلامة إضافية

- قراءة نتائج Query تُستخدم مباشرة دون إعادة `getDoc` لكل مستند، لتجنب مضاعفة القراءات في عمليات النسخ والحذف.
- كاش metadata الداخلي قصير العمر، 15 ثانية، حتى يستفيد تسلسل القراءة ثم الكتابة من نفس البيانات دون الاحتفاظ بنسخة قديمة طويلاً.
- حد جلب سجل التدقيق القادم من Query مقيد برمجياً لمنع طلب غير محدود عن طريق معاملات الرابط.
- نتيجة Dataset غير الموجود تدخل في كاش الصلاحية، فلا يعاد طلب المستند المفقود قبل انتهاء مدة الكاش.

## حدود الاختبار

لم يتم تنفيذ اختبار تكاملي مدفوع على مشروع Firebase الفعلي لأن بيانات الاعتماد وقواعد الإنتاج ليست جزءاً من بيئة الاختبار المحلية. بعد النشر، يوصى بمراقبة لوحة Firebase Usage لمدة يوم عمل ومقارنة قراءات وكتابات Firestore قبل Revision 70 وبعده.
