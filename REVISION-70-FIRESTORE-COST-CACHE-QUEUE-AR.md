# Revision 70 — ترشيد Firestore والكاش وطابور المزامنة

تستند هذه الحزمة إلى Revision 69 / R63، مع إبقاء بنية تسجيل الدخول وعزل الشركات كما هي. يركّز هذا الإصدار على خفض قراءات وكتابات Cloud Firestore ومنع عملية متعثرة من تعطيل طابور المزامنة.

## 1. تقليل القراءات والكاش

- إضافة كاش صلاحية دائم لكل Dataset في `localStorage`، مرتبط بمعرّف الشركة، ومدته الافتراضية دقيقتان.
- الصفحة تستخدم بياناتها المحلية فوراً، ولا تقرأ Dataset من Firestore ما دامت نتيجة الفحص حديثة.
- يتم تخزين نتيجة المستند غير الموجود أيضاً؛ لذلك لا تتكرر قراءة المستندات المفقودة في كل دورة.
- رفع فترة فحص Firestore النشط من 3 ثوانٍ إلى 120 ثانية.
- السحب الكامل في الخلفية أصبح كل 15 دقيقة بدلاً من تنفيذه بعد كل فتح أو مزامنة قصيرة.
- بيانات الصفحة تُسحب فقط حسب الأولوية، بينما السحب الكامل يتم على دفعات.
- قراءة مجموعات Firestore الكبيرة أصبحت باستخدام `limit()` و`startAfter()` وبحجم صفحة افتراضي 20 مستنداً.
- جلب سجل التدقيق ليوم كامل أصبح بطلب واحد بدلاً من 24 طلباً منفصلاً للساعات.
- تنظيف سجل التدقيق أصبح كل 6 ساعات كحد أقصى بدلاً من كل فتح صفحة.
- تفعيل `persistentLocalCache` متعدد التبويبات في مسار إدارة قواعد Firestore المتعددة، مع إبقاء WebView Android على كاش الذاكرة للاستقرار.
- جسر Firestore Lite الرئيسي يعتمد على كاش التطبيق المحلي وطابور IndexedDB، لتجنب مشكلة SDK الداخلية التي عالجها Revision 69.

## 2. تقليل الكتابات وحجم التخزين

- تجميع تغييرات الواجهة باستخدام Debounce افتراضي 1200ms قبل تشغيل المزامنة.
- مقارنة `contentHash` وحجم JSON قبل الكتابة؛ إذا لم تتغير البيانات فلا تتم كتابة جديدة.
- البيانات الصغيرة تحفظ في مستند واحد بدلاً من إنشاء مستند إصدار وعدة Chunks لكل تعديل.
- البيانات الكبيرة فقط تستخدم التخزين المجزأ، وتُكتب Chunks عبر Write Batches.
- تحديث مستندات metadata والفهارس وملخصات الشركات يستخدم `updateDoc` مع إنشاء المستند عند عدم وجوده.
- PATCH الخاص بـ metadata لم يعد ينفذ GET ثم PUT في كل مزامنة.
- منع حفظ `data:*;base64` داخل مستندات Firestore. صفحات الشعار وتصميم الفاتورة تحفظ روابط HTTPS/Firebase Storage فقط.
- تبقى عمليات إنشاء صور الطباعة أو التنزيل محلية ومؤقتة، ولا تدخل ضمن البيانات المتزامنة.

## 3. طابور المزامنة والأخطاء

- كل Dataset تتم مزامنته داخل `try/catch` مستقل؛ فشل عنصر لا يمنع العناصر التالية.
- أخطاء الشبكة المؤقتة تبقى في الطابور مع Backoff وإعادة محاولة لاحقة.
- الأخطاء الدائمة مثل `permission-denied` و`invalid-argument` و`failed-precondition` تُنقل إلى قائمة فشل محلية منفصلة ثم تُحذف من الطابور النشط.
- ينطبق العزل نفسه على سجل التدقيق؛ السجل المرفوض يتم تخطيه وتستمر بقية السجلات.
- تمت إضافة واجهات برمجية محلية لفحص العمليات المعزولة وإعادة إدراج عمليات Dataset أو مسحها:
  - `CashtopFirebase.getFailedOperations()`
  - `CashtopFirebase.retryFailedOperations(ids)`
  - `CashtopFirebase.clearFailedOperations(ids)`

## 4. إدارة المستمعين ودورة حياة الصفحة

- مسار Firestore الحالي لا يستخدم `onSnapshot`؛ لذلك لا توجد مستمعات Firestore دائمة تستهلك قراءات في الخلفية.
- مستمع SSE الخاص بمسار RTDB الاحتياطي يُغلق عند إخفاء الصفحة وعند `pagehide`.
- مؤقتات المزامنة والسحب الكامل والفحص الدوري تُلغى عند مغادرة الصفحة.

## الإعدادات الافتراضية

يمكن تعديل القيم في `firebase-config.js` داخل `sync` و`firestore`:

- `datasetCacheTtlMs`: دقيقتان.
- `activePollMs`: دقيقتان.
- `backgroundFullPullMs`: 15 دقيقة.
- `writeDebounceMs`: 1200ms.
- `pageSize`: 20 مستنداً.
- `inlineJsonMaxBytes`: 720000 بايت.
- `auditRecentLimit`: 100 سجل.
- `auditRangeLimit`: 500 سجل.

## ملاحظة Firebase Storage

الكود يمنع إدخال Base64 إلى Firestore، ويطلب رابط صورة مباشر. يجب رفع الصور إلى Firebase Storage أو خدمة ملفات HTTPS، ثم حفظ رابط التنزيل. تأكد من إعداد Storage Rules وCORS بما يتوافق مع صلاحيات التطبيق.
