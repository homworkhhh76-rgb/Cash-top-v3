# Revision 60 — Firestore متعدد القواعد

- اعتماد مشروع `ahmed-97701` كقاعدة إدارة رئيسية بإعدادات Firebase التي زود بها المستخدم.
- إضافة نموذج كامل في `admin.html` لإدخال اسم القاعدة وحقول `firebaseConfig` و`Database ID`.
- إضافة طبقة `multi-database.js` لتهيئة أكثر من مشروع Firebase أو أكثر من قاعدة Firestore مسماة.
- حفظ تعريفات القواعد في `cashtopDatabases` ومسارات المفاتيح في `cashtopDatabaseRoutes`.
- نقل تخزين الشركات والمفاتيح التشغيلية إلى قاعدة الشركة المختارة.
- تجميع وعرض مفاتيح جميع القواعد داخل لوحة الأدمن مع التحكم بالتفعيل والحذف والتعديل.
- تسجيل الدخول يحدد قاعدة المفتاح من القاعدة الرئيسية، ويتحقق من قاعدة الشركة، ثم يحفظ إعدادها في الكاش.
- دعم وجود عدة مفاتيح وشركات داخل قاعدة واحدة، ومنع تكرار المفتاح بين القواعد.
- تحديث بوابة العملاء لتحديد قاعدة الشركة من المفتاح.
- تحديث النسخ الاحتياطي ليشمل جميع القواعد والشركات.
- إزالة جميع نقاط تشغيل MongoDB ومسار النقل القديم من ملفات التشغيل.
- فصل قواعد الأمان: `firestore.rules` للقاعدة الرئيسية و`firestore-company.rules` لقواعد الشركات، لمنع خلط البيانات التشغيلية مع سجل الإدارة.
